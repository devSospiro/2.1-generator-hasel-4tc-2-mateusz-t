<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- Identyfikator motywu -->
    <sys:String x:Key="ThemeName">Light</sys:String>
    <sys:String x:Key="ThemeIcon">🌙</sys:String>
    <sys:String x:Key="ThemeTooltip">Przełącz na tryb ciemny</sys:String>

    <!-- Kolory bazowe -->
    <Color x:Key="BackgroundColor">#F3F4F8</Color>
    <Color x:Key="SurfaceColor">#FFFFFF</Color>
    <Color x:Key="CardColor">#FFFFFF</Color>
    <Color x:Key="CardHoverColor">#F8F9FD</Color>
    <Color x:Key="InputBackgroundColor">#F8FAFC</Color>
    <Color x:Key="BorderColor">#E2E8F0</Color>
    <Color x:Key="BorderFocusColor">#6366F1</Color>

    <!-- Kolory akcentów -->
    <Color x:Key="AccentColor">#4F46E5</Color>
    <Color x:Key="AccentHoverColor">#6366F1</Color>
    <Color x:Key="AccentPressedColor">#4338CA</Color>

    <!-- Kolory tekstu -->
    <Color x:Key="TextPrimaryColor">#0F172A</Color>
    <Color x:Key="TextSecondaryColor">#475569</Color>
    <Color x:Key="TextMutedColor">#94A3B8</Color>

    <!-- Kolory statusów -->
    <Color x:Key="SuccessColor">#059669</Color>
    <Color x:Key="WarningColor">#D97706</Color>
    <Color x:Key="DangerColor">#DC2626</Color>

    <!-- Pędzle SolidColor -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="CardBrush" Color="{StaticResource CardColor}" />
    <SolidColorBrush x:Key="CardHoverBrush" Color="{StaticResource CardHoverColor}" />
    <SolidColorBrush x:Key="InputBackgroundBrush" Color="{StaticResource InputBackgroundColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="BorderFocusBrush" Color="{StaticResource BorderFocusColor}" />

    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
    <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}" />
    <SolidColorBrush x:Key="AccentPressedBrush" Color="{StaticResource AccentPressedColor}" />

    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}" />
    <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}" />

    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" />
    <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}" />

    <SolidColorBrush x:Key="WindowBorderBrush" Color="#CBD5E1" />
    <SolidColorBrush x:Key="ControlBorderBrush" Color="#CBD5E1" />

    <!-- Pędzle paska przewijania (ScrollBar) -->
    <SolidColorBrush x:Key="ScrollBarTrackBrush" Color="Transparent" />
    <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="#CBD5E1" />
    <SolidColorBrush x:Key="ScrollBarThumbHoverBrush" Color="#4F46E5" />

    <!-- Gradienty tła i akcentu -->
    <LinearGradientBrush x:Key="WindowGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#EEF2F6" Offset="0" />
        <GradientStop Color="#F3F4F8" Offset="0.5" />
        <GradientStop Color="#E8ECF4" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PrimaryGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#4F46E5" Offset="0" />
        <GradientStop Color="#7C3AED" Offset="0.5" />
        <GradientStop Color="#9333EA" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PrimaryHoverGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#6366F1" Offset="0" />
        <GradientStop Color="#8B5CF6" Offset="0.5" />
        <GradientStop Color="#A855F7" Offset="1" />
    </LinearGradientBrush>

    <!-- Cień -->
    <Color x:Key="CardShadowColor">#15000000</Color>

</ResourceDictionary>
